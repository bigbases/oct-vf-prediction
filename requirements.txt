# Python 3.10.18
#
# The image branch was trained with CUDA 11.3 wheels:
#   pip install --extra-index-url https://download.pytorch.org/whl/cu113 \
#       torch==1.12.1+cu113 torchvision==0.13.1+cu113
# The plain pins below install the CPU builds; use the line above for GPU.
#
# An equivalent conda specification is in environment.yml.

torch==1.12.1
torchvision==0.13.1
timm==1.0.27
xgboost==3.2.0
numpy==1.26.4
scipy==1.15.2
scikit-learn==1.7.2
matplotlib==3.9.4
Pillow==12.2.0
PyYAML==6.0.2

# Report extraction only (extract_cirrus.py, ocr_*.py).
# These also need the system binaries `tesseract` and `poppler-utils`.
pytesseract==0.3.13
pdf2image==1.17.0
